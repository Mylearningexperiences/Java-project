public class Rectangulotor {
   public static void main(String[] args) {
      int length= Integer.parseInt(args[0]);
      int width = Integer.parseInt(args[1]);
        
      rectangle myRectangle = new Ractangle(length,width);
      
     String output = String. format("*** your Ractangle ***\n\nLength: %d\nWidth: %d\nArea: %d\nPerimeter: %d\n\n", myRectangle.length, myRectangle.width, myRactangle.getArea(), myRectangle.getPerimeter());
 
     System.out.println(output);

 }
   }

  
